- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/config']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline.conf: tmuxline.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
